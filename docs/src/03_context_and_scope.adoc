ifndef::imagesdir[:imagesdir: ../images]

[[section-context-and-scope]]
== Contexto y Alcance

=== Contexto general

YOVI es una plataforma web diseñada por la empresa Micrati para jugar a diferentes variantes del JUEGO Y, inspiradas en juegos de tablero estratégicos como Go o Hex, permitiendo a los usuarios enfrentarse entre sí o contra la máquina, con un tablero de tamaño variable y reglas específicas según la variante seleccionada.

El sistema gestiona usuarios registrados, permite consultar estadísticas de partidas, y soporta la selección de estrategias y niveles de dificultad para el juego contra la máquina.

Los principales objetivos de YOVI son los siguientes:

* Proporcionar una experiencia de juego fluida y coherente, garantizando que cada acción del jugador se valide correctamente y se refleje en el estado del tablero de forma inmediata.
* Fomentar enfrentamientos amistosos con partidas multijugador (2 jugadores) y con un ranking de usuarios a partir de la puntuación obtenida.
* Contribuir al entretenimiento y al desarrollo estratégico por las reglas del juego, fácil de entender gracias a la interfaz.

El alcance inicial del sistema se centra en la versión clásica del juego Y, con partidas de un jugador contra la máquina y soporte para diferentes estrategias y niveles de dificultad. No se incluyen funcionalidades avanzadas como partidas simultáneas con varios jugadores, chat en tiempo real o ranking complejo de usuarios, aunque la arquitectura se plantea de forma que estas funcionalidades puedan incorporarse en futuras versiones sin necesidad de reestructurar el sistema.

=== Contexto técnico

YOVI se desarrolla como una aplicación web compuesta por distintos módulos y múltiples tecnologías.

Los componentes principales de la aplicación son:

* **El frontend web:** desarrollado en React, se ejecuta en el navegador del usuario y se encarga de la presentación visual del tablero, la gestión de interacciones de los jugadores y la comunicación con el backend.
* **El backend:** se divide en dos microservicios independientes que gestionan distintas responsabilidades del sistema.
**El microservicio GameY**, implementado en Rust, centraliza toda la lógica de juego. Su responsabilidad incluye validar movimientos, controlar los turnos, calcular capturas y determinar el resultado final de la partida. Al ser la parte que determina el estado del tablero, se asegura de prevenir que el usuario pueda corromper el juego.
**El microservicio Users**, en Node.js, gestiona toda la información relacionada con los usuarios del sistema. Sus responsabilidades incluyen el registro de usuarios, la consulta del historial de partidas y la gestión de estadísticas.
* **La persistencia de datos:** se realiza mediante MongoDB, que almacena información estructurada de usuarios, partidas y resultados. Elegimos el modelo noSQL por la facilidad de despliegue y la capacidad de ver los datos en tiempo real.
* **La comunicación entre frontend y backend:** se realiza mediante mensajes JSON siguiendo la notación YEN. Esta notación permite representar de manera precisa la situación de cada partida en un instante dado, facilitando tanto la interacción con la interfaz web como la integración con bots a través del API.
* **El sistema se despliega en un entorno contenerizado:** mediante Docker, lo que asegura reproducibilidad y consistencia en el entorno de ejecución. La aplicación se ejecuta en una máquina virtual en la nube, proporcionando disponibilidad remota y facilitando el acceso para usuarios y pruebas. 

La comunicación se realiza mediante una API REST que intercambia mensajes JSON.. Esta arquitectura modular y distribuida permite que el sistema sea escalable y extensible para futuras versiones que incluyan variantes adicionales del juego, soporte para varios jugadores o funcionalidades opcionales avanzadas.

= image:yovi-estructura.png[componentes-yovi]
